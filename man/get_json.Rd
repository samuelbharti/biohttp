% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perform.R
\name{get_json}
\alias{get_json}
\title{GET a JSON endpoint}
\usage{
get_json(
  base_url,
  path = NULL,
  query = list(),
  source = "API",
  timeout = 15,
  max_tries = 3,
  headers = NULL,
  throttle = NULL,
  secret_query = NULL
)
}
\arguments{
\item{base_url}{The service's base URL.}

\item{path}{An optional path appended to \code{base_url}.}

\item{query}{A named list of query parameters. Blank values are dropped.}

\item{source}{A friendly label for the service.}

\item{timeout}{Seconds before the request is abandoned.}

\item{max_tries}{Total attempts, including the first.}

\item{headers}{A named list of headers, all marked sensitive.}

\item{throttle}{An optional throttle spec. See \code{\link[=req_defaults]{req_defaults()}}.}

\item{secret_query}{A named list of query-string credentials, for a service
that has no header form. Deliberately \strong{not} part of the cache key, so it
suits a credential that raises a rate limit and not one that changes the
response. See \code{\link[=redact_secrets]{redact_secrets()}}.}
}
\value{
An envelope. See \code{\link[=envelope]{envelope()}}.
}
\description{
Assembles the request, applies \code{\link[=req_defaults]{req_defaults()}}, performs it, and caches the
result if it succeeded.
}
\examples{
# Mocked so the example runs offline. A real call drops the
# with_mocked_responses() wrapper.
cache_reset()
breaker_reset()

httr2::with_mocked_responses(
  list(httr2::response(
    status_code = 200,
    headers = list(`content-type` = "application/json"),
    body = charToRaw('{"hits":[{"symbol":"BRCA1"}]}')
  )),
  get_json(
    "https://mygene.info/v3",
    path = "query",
    query = list(q = "BRCA1", species = "human"),
    source = "MyGene"
  )$data$hits[[1]]$symbol
)

}

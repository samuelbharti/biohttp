% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{post_json_many}
\alias{post_json_many}
\title{POST many JSON bodies as one batch}
\usage{
post_json_many(
  url,
  bodies = list(),
  source = "API",
  timeout = 20,
  max_tries = 3,
  headers = NULL,
  throttle = NULL,
  max_active = 6,
  progress = FALSE
)
}
\arguments{
\item{url}{The endpoint URL.}

\item{bodies}{A list of request bodies, one per request. Each is serialized
as JSON.}

\item{source}{A friendly label for the service, used in the user-facing
message. For example \code{"MyGene"}.}

\item{timeout}{Seconds before a request is abandoned.}

\item{max_tries}{Total attempts, including the first. Note that httr2 does
not honor this under parallel performance.}

\item{headers}{A named list of headers, all marked sensitive.}

\item{throttle}{A throttle spec. See \code{\link[=req_defaults]{req_defaults()}}.}

\item{max_active}{Maximum requests in flight at once.}

\item{progress}{Passed to \code{httr2::req_perform_parallel()}. \code{FALSE} by
default, because the usual caller is a Shiny app that renders its own.}
}
\value{
A list of envelopes, the same length and order as \code{bodies}.
}
\description{
The batched counterpart to \code{\link[=post_json]{post_json()}}, for a JSON or GraphQL endpoint
answering many queries. Keyed on the URL and the body together, so two
different queries to the same endpoint do not collide.
}
\details{
Read the sections on \code{\link[=perform_many]{perform_many()}} first.
}
\examples{
cache_reset()
breaker_reset()

httr2::with_mocked_responses(
  function(req) {
    httr2::response(
      status_code = 200,
      headers = list(`content-type` = "application/json"),
      body = charToRaw('{"data":{}}')
    )
  },
  length(post_json_many(
    "https://gnomad.broadinstitute.org/api",
    bodies = list(list(query = "{ a }"), list(query = "{ b }")),
    source = "gnomAD"
  ))
)

}

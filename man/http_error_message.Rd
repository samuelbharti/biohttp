% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/errors.R
\name{http_error_message}
\alias{http_error_message}
\title{A user-facing sentence for a failed call}
\usage{
http_error_message(source, http = NA_integer_, condition = NULL)
}
\arguments{
\item{source}{A friendly label for the service, for example \code{"gnomAD"}.}

\item{http}{An HTTP status code, or \code{NA_integer_} when no response arrived.}

\item{condition}{A caught condition, used only when \code{http} is \code{NA}.}
}
\value{
A single string.
}
\description{
Works out which status the failure is and asks \code{\link[=status_message]{status_message()}} for the
wording, so it honours \code{biohttp.status_message} like everything else. Pass
\code{http} for a response that arrived, or \code{condition} for a transport failure
where none did.
}
\examples{
http_error_message("gnomAD", http = 404L)
http_error_message("gnomAD", http = 503L)
http_error_message("gnomAD", condition = simpleError("Timeout was reached"))

}

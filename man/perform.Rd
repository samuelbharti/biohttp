% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perform.R
\name{perform}
\alias{perform}
\title{Perform a request and normalize the result}
\usage{
perform(req, source = "API")
}
\arguments{
\item{req}{An httr2 request, prepared with \code{\link[=req_defaults]{req_defaults()}}.}

\item{source}{A friendly label for the service, used in the user-facing
message. For example \code{"gnomAD"}.}
}
\value{
An envelope. See \code{\link[=envelope]{envelope()}}.
}
\description{
Never raises on an HTTP or a parse failure. Returns an envelope for every
outcome, so a caller branches on \code{res$status} and writes no \code{tryCatch()} of
its own.
}
\details{
Short-circuits to a \code{skipped} envelope without sending anything when the
host's breaker is open. See \code{\link[=breaker_open]{breaker_open()}}.

The request must have been through \code{\link[=req_defaults]{req_defaults()}}, which disarms httr2's
error raising. Without that, \code{req_perform()} throws on a non-2xx and a
perfectly reachable host is recorded as a transport failure.
}
\examples{
breaker_reset()
req <- req_defaults(httr2::request("https://mock.test/x"))

httr2::with_mocked_responses(
  list(httr2::response(
    status_code = 200,
    headers = list(`content-type` = "application/json"),
    body = charToRaw('{"symbol":"BRCA1"}')
  )),
  perform(req, "MyGene")$data$symbol
)

}

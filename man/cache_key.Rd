% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cache_key}
\alias{cache_key}
\title{Build a cache key}
\usage{
cache_key(source, key, params = NULL)
}
\arguments{
\item{source}{A friendly label for the service.}

\item{key}{Something identifying the call, usually the method and URL.}

\item{params}{Anything else that changes the answer, such as a POST body.}
}
\value{
A single string.
}
\description{
A stable lowercase-hex hash. \code{cachem} keys may contain only lowercase
letters and numbers, so a URL cannot be one directly and the inputs are
hashed instead. \code{rlang::hash()} returns lowercase hex, which satisfies the
rule.
}
\details{
The salt and the schema version are inside the hash, so changing either
yields a fresh keyspace. Set \code{BIOHTTP_CACHE_SALT} per deployment: a shared
disk cache would otherwise collide across app versions, and it would leak
which queries were run to anyone able to probe it.

\strong{Put anything that changes the answer into \code{params}, credentials
included.} The wrappers pass their \code{headers} through, because two callers
hitting the same URL with different tokens can legitimately get different
responses, and keying on the URL alone would serve one caller's data to the
other. The cost is that rotating a token misses the cache once, which is the
right trade.
}
\examples{
cache_key("gnomAD", "GET https://example.org/v1/gene/BRCA1")
cache_key("gnomAD", "POST https://example.org/graphql", list(q = "BRCA1"))

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/envelope.R
\name{envelope}
\alias{envelope}
\title{Build a result envelope}
\usage{
envelope(
  status,
  data = NULL,
  source = "API",
  http = NA_integer_,
  error = NULL,
  detail = NULL,
  retry_after = NA_real_
)
}
\arguments{
\item{status}{One of \link{STATUS_LEVELS}.}

\item{data}{The parsed body on success, \code{NULL} otherwise.}

\item{source}{A friendly label for the service, used in the user-facing
message. For example \code{"gnomAD"}.}

\item{http}{The HTTP status code, or \code{NA_integer_} when no response arrived.}

\item{error}{One sentence fit to show a user. Never carries technical
detail.}

\item{detail}{The technical cause, for a log. Never shown to a user.}

\item{retry_after}{Seconds a \code{Retry-After} header asked the caller to
wait, or \code{NA_real_} when the response carried none. Diagnostic: nothing
in this package reads it back except the batched retry path in
\code{parallel.R}, which shares this same field.}
}
\value{
A list with \code{ok}, \code{status}, \code{http}, \code{data}, \code{source}, \code{error},
\code{detail}, \code{retry_after}, and \code{ts}.
}
\description{
The single return shape for the whole package. \code{ok} is derived from \code{status}
rather than passed in, so the two can never disagree.
}
\details{
A client that only wants the parsed body can use \code{\link[=body_or_null]{body_or_null()}} instead of
reading the fields directly.
}
\examples{
envelope("ok", data = list(symbol = "BRCA1"), source = "MyGene", http = 200L)
envelope("error", source = "gnomAD", http = 503L)

}

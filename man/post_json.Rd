% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perform.R
\name{post_json}
\alias{post_json}
\title{POST a JSON body}
\usage{
post_json(
  url,
  body,
  source = "API",
  timeout = 20,
  max_tries = 3,
  headers = NULL,
  throttle = NULL,
  secret_query = NULL,
  ttl = NULL
)
}
\arguments{
\item{url}{The endpoint URL.}

\item{body}{A list, serialized as the JSON request body.}

\item{source}{A friendly label for the service.}

\item{timeout}{Seconds before the request is abandoned.}

\item{max_tries}{Total attempts, including the first.}

\item{headers}{A named list of headers, all marked sensitive.}

\item{throttle}{An optional throttle spec. See \code{\link[=req_defaults]{req_defaults()}}.}

\item{secret_query}{A named list of query-string credentials, for a service
that has no header form. Deliberately \strong{not} part of the cache key, so it
suits a credential that raises a rate limit and not one that changes the
response. See \code{\link[=redact_secrets]{redact_secrets()}}.}

\item{ttl}{Seconds a cached success stays fresh. \code{NULL}, the default, means
the configured lifetime from \code{BIOHTTP_CACHE_TTL}. See \code{\link[=cached]{cached()}}.}
}
\value{
An envelope. See \code{\link[=envelope]{envelope()}}.
}
\description{
For a JSON or GraphQL POST. Caches on the URL and the body together, so two
different queries to the same endpoint do not collide.
}
\examples{
cache_reset()
breaker_reset()

httr2::with_mocked_responses(
  list(httr2::response(
    status_code = 200,
    headers = list(`content-type` = "application/json"),
    body = charToRaw('{"data":{"gene":{"gene_id":"ENSG00000012048"}}}')
  )),
  post_json(
    "https://gnomad.broadinstitute.org/api",
    body = list(query = "{ gene(gene_symbol: \"BRCA1\") { gene_id } }"),
    source = "gnomAD"
  )$data$data$gene$gene_id
)

}

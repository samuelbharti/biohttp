% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/breaker.R
\name{breaker_open}
\alias{breaker_open}
\title{Is a host's breaker open}
\usage{
breaker_open(host, now = as.numeric(Sys.time()))
}
\arguments{
\item{host}{A hostname, as returned by parsing a request URL.}

\item{now}{The current time in seconds since the epoch. Exposed so a test can
drive the clock rather than sleep.}
}
\value{
A single logical.
}
\description{
Open means too many consecutive transport failures and still inside the
cooldown. While open, \code{\link[=perform]{perform()}} short-circuits to a \code{skipped} envelope
instead of waiting on a request that is likely to fail anyway.
}
\examples{
breaker_reset()
breaker_open("gnomad.broadinstitute.org")

}

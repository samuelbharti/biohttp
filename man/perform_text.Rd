% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perform.R
\name{perform_text}
\alias{perform_text}
\title{Perform a request and return the body as text}
\usage{
perform_text(req, source = "API", secret_query = NULL)
}
\arguments{
\item{req}{An httr2 request, prepared with \code{\link[=req_defaults]{req_defaults()}}.}

\item{source}{A friendly label for the service, used in the user-facing
message. For example \code{"gnomAD"}.}

\item{secret_query}{A named list of query-string credentials. Appended here
rather than by the caller, so \code{req} itself never carries them and neither
does anything built from \code{req$url}. See \code{\link[=redact_secrets]{redact_secrets()}}.}
}
\value{
An envelope whose \code{data} is a single string on success.
}
\description{
The non-JSON counterpart to \code{\link[=perform]{perform()}}, for a source that ships a bulk flat
file rather than a per-record JSON API. The body lands in \code{data} as a single
string, so the envelope shape is the same either way and a caller does not
branch on which wrapper it used.
}
\details{
The breaker rule is the same one: only a transport failure counts against the
host.
}
\examples{
breaker_reset()
req <- req_defaults(httr2::request("https://mock.test/genes.tsv"))

httr2::with_mocked_responses(
  list(httr2::response(
    status_code = 200,
    body = charToRaw("gene\tscore\n")
  )),
  perform_text(req, "ClinGen")$data
)

}

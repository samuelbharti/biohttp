% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{get_text_many}
\alias{get_text_many}
\title{GET many text endpoints as one batch}
\usage{
get_text_many(
  base_url,
  path = NULL,
  queries = list(),
  source = "API",
  timeout = 30,
  max_tries = 3,
  headers = NULL,
  throttle = NULL,
  max_active = 6,
  progress = FALSE,
  secret_query = NULL,
  ttl = NULL
)
}
\arguments{
\item{base_url}{The service's base URL.}

\item{path}{A path appended to \code{base_url}. Length 1 to use the same path for
every request, or one per entry in \code{queries}.}

\item{queries}{A list of named lists, one per request. Blank values are
dropped from each.}

\item{source}{A friendly label for the service, used in the user-facing
message. For example \code{"MyGene"}.}

\item{timeout}{Seconds before a request is abandoned.}

\item{max_tries}{Total attempts, including the first. A retry pass
re-sends only the entries that came back \code{rate_limited}, \code{timeout}, or
a 5xx, honoring \code{Retry-After} between passes. See the retry section on
\code{\link[=perform_many]{perform_many()}}.}

\item{headers}{A named list of headers, all marked sensitive.}

\item{throttle}{A throttle spec. See \code{\link[=req_defaults]{req_defaults()}}.}

\item{max_active}{Maximum requests in flight at once.}

\item{progress}{Passed to \code{httr2::req_perform_parallel()}. \code{FALSE} by
default, because the usual caller is a Shiny app that renders its own.}

\item{secret_query}{A named list of query-string credentials, applied to
every request in the batch at dispatch. See \code{\link[=redact_secrets]{redact_secrets()}}.}

\item{ttl}{Seconds a cached success stays fresh, applied to every entry in
the batch. \code{NULL}, the default, means the configured lifetime from
\code{BIOHTTP_CACHE_TTL}. See \code{\link[=cached]{cached()}}.}
}
\value{
A list of envelopes, the same length and order as \code{queries}, each
with a single string in \code{data} on success.
}
\description{
The batched counterpart to \code{\link[=get_text]{get_text()}}, for a source that ships several
flat files rather than one. Each body comes back verbatim as a string in
\code{data}, so the envelope shape matches the single call and a caller does not
branch on which wrapper it used.
}
\details{
The keys are built exactly the way \code{\link[=get_text]{get_text()}} builds its own, so a batch
reuses a file a single call already fetched and the other way around.

Read the sections on \code{\link[=perform_many]{perform_many()}} first. The same two rules apply: pass
requests for one host, and supply a \code{throttle}.
}
\examples{
cache_reset()
breaker_reset()

httr2::with_mocked_responses(
  function(req) {
    httr2::response(
      status_code = 200,
      body = charToRaw("gene\tscore\n")
    )
  },
  length(get_text_many(
    "https://search.clinicalgenome.org",
    path = c("kb/gene-validity/download", "kb/dosage/download"),
    queries = list(list(), list()),
    source = "ClinGen"
  ))
)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratelimit.R
\name{ratelimit_record}
\alias{ratelimit_record}
\title{Record a pause for a host}
\usage{
ratelimit_record(host, retry_after = NULL, now = as.numeric(Sys.time()))
}
\arguments{
\item{host}{A hostname, as returned by parsing a request URL.}

\item{retry_after}{Seconds to wait, usually from
\code{\link[httr2:resp_retry_after]{httr2::resp_retry_after()}}. \code{NULL} or \code{NA} falls back to the default
pause.}

\item{now}{The current time in seconds since the epoch. Exposed so a test
can drive the clock rather than sleep.}
}
\value{
The host's new \code{pause_until}, invisibly.
}
\description{
Sets how long the next dispatch to \code{host} should wait: from a 429 or
503's \code{Retry-After} when one was sent, or a default pause (5 seconds,
\code{BIOHTTP_RATELIMIT_DEFAULT_PAUSE} to override) otherwise. A call only
extends an existing pause, never shortens one, so of several retryable
responses recorded from one batch, the longest asked-for wait wins.
}
\examples{
ratelimit_reset()
ratelimit_record("rest.ensembl.org", retry_after = 5)
ratelimit_reset()

}

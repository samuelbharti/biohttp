% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/envelope.R
\name{status_constructors}
\alias{status_constructors}
\alias{status_ok}
\alias{status_no_data}
\alias{status_stale}
\alias{status_rate_limited}
\alias{status_timeout}
\alias{status_skipped}
\alias{status_error}
\title{Envelope constructors, one per status}
\usage{
status_ok(data, source = "API", http = 200L)

status_no_data(source = "API", http = NA_integer_, detail = NULL)

status_stale(data, source = "API", detail = NULL)

status_rate_limited(source = "API", http = 429L, detail = NULL)

status_timeout(source = "API", detail = NULL)

status_skipped(source = "API", detail = NULL)

status_error(source = "API", http = NA_integer_, error = NULL, detail = NULL)
}
\arguments{
\item{data}{The parsed body on success, \code{NULL} otherwise.}

\item{source}{A friendly label for the service, used in the user-facing
message. For example \code{"gnomAD"}.}

\item{http}{The HTTP status code, or \code{NA_integer_} when no response arrived.}

\item{detail}{The technical cause, for a log. Never shown to a user.}

\item{error}{One sentence fit to show a user. Never carries technical
detail.}
}
\value{
An envelope list, as described in \code{\link[=envelope]{envelope()}}.
}
\description{
Shorthands for \code{\link[=envelope]{envelope()}} that fill in the user-facing \code{error} sentence for
their status. \code{error} is what a user reads; \code{detail} is what a log records.
}
\details{
The sentence comes from \code{\link[=status_message]{status_message()}}, so setting
\code{biohttp.status_message} changes what these produce too.
}
\examples{
status_ok(data = list(n = 1), source = "MyGene")
status_no_data(source = "gnomAD", http = 404L)
status_skipped(source = "gnomAD", detail = "gnomad.broadinstitute.org open")

}

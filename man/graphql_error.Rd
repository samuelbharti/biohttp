% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/errors.R
\name{graphql_error}
\alias{graphql_error}
\title{Catch a GraphQL query error inside a 200}
\usage{
graphql_error(res, source = "API")
}
\arguments{
\item{res}{An envelope from \code{\link[=post_json]{post_json()}} or \code{\link[=perform]{perform()}}.}

\item{source}{A friendly label for the service.}
}
\value{
The failing envelope, or \code{NULL} when the call genuinely succeeded.
}
\description{
GraphQL reports query errors in the body of an HTTP 200, as a top-level
\code{errors} array, so a 2xx status is not enough to call a call successful.
}
\details{
Returns an error envelope for either kind of failure, transport or query, so
a GraphQL client collapses the two checks into one:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{res <- post_json(url, body, source = "gnomAD")
bad <- graphql_error(res, "gnomAD")
if (!is.null(bad)) return(bad)
}\if{html}{\out{</div>}}
}
\examples{
clean <- status_ok(data = list(data = list(gene = "BRCA1")), source = "G")
graphql_error(clean, "G")

queried <- status_ok(data = list(errors = list(list(message = "bad"))))
graphql_error(queried, "G")$status

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratelimit.R
\name{ratelimit_wait}
\alias{ratelimit_wait}
\title{Wait out a host's recorded pause}
\usage{
ratelimit_wait(host, now = as.numeric(Sys.time()), sleep = Sys.sleep)
}
\arguments{
\item{host}{A hostname.}

\item{now}{The current time in seconds since the epoch.}

\item{sleep}{The sleep function to call with the remaining seconds.
Defaults to \code{\link[=Sys.sleep]{Sys.sleep()}}.}
}
\value{
The number of seconds waited (\code{0} when there was nothing to wait
for), invisibly.
}
\description{
A no-op when the host has no pause recorded, or the recorded one has
already elapsed. \code{sleep} is injectable so a test can assert the duration
asked for without actually waiting for it.
}
\examples{
ratelimit_reset()
ratelimit_wait("rest.ensembl.org")

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/request.R
\name{req_defaults}
\alias{req_defaults}
\title{Apply the shared request options}
\usage{
req_defaults(
  req,
  timeout = 20,
  max_tries = 3,
  headers = NULL,
  throttle = NULL,
  user_agent = default_user_agent()
)
}
\arguments{
\item{req}{An httr2 request.}

\item{timeout}{Seconds before the request is abandoned.}

\item{max_tries}{Total attempts, including the first.}

\item{headers}{A named list of headers. All of them are marked sensitive.}

\item{throttle}{An optional list with \code{capacity}, \code{fill_time_s}, and \code{realm}.
httr2 token buckets are per process, so a realm keyed on the host gives one
honest bucket per host. Defaults to the request's own host.}

\item{user_agent}{The User-Agent string. See \code{\link[=user_agent]{user_agent()}}.}
}
\value{
The request, with the options applied.
}
\description{
Timeout, bounded retry on transient failures, an attributable user agent, and
optionally a throttle and headers.
}
\details{
\code{req_error()} is disarmed here on purpose. httr2 would otherwise raise on a
non-2xx, and this package normalizes every outcome into an envelope instead,
so \code{\link[=perform]{perform()}} needs the response object rather than a condition.

Headers are attached with \code{.redact} set to their names, so a token never
prints in an inspected request, a log line, or an error message. Header auth
also keeps the secret out of the URL, where it would end up in access logs.
}
\examples{
req_defaults(httr2::request("https://example.org"), timeout = 5)

# A key-gated source. The token is redacted from anything printable.
req_defaults(
  httr2::request("https://example.org"),
  headers = list(Authorization = "Bearer secret-token")
)

}
